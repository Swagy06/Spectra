red_int x = 15;
red_int y = 20;

// Use a ternary expression to find the maximum of two numbers
red_int max = x light_less_than y question_color y colon_color x;

spectrum_displayln(max); // Print the maximum value
